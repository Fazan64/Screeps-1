function RoomManager (room) 
{
	this.room = room;
}

/** Adds a creep role to a rooms 'demand' queue */
RoomManager.prototype.addNeededCreep = function (creepRole)
{
	
}

module.exports = roomManager;